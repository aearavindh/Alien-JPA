stages:
  - build
  - execute

build:
  stage: build
  tags:
     - java-ci
  script: mvn clean install
  artifacts:
    paths:
     - target/
execute:
  stage: execute
  tags:
     - java-ci
  script: mvn spring-boot:run
